"custom_net_tables"
{
    "player_stats"
    {
    }

    // Holds post-game results for each player by PlayerID (as string keys).
    // Example keys: "old", "new", "name", "team", "doubledown"
    "postgame"
    {
    }
}
